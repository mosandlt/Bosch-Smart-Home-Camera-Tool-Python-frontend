nicegui>=3.12.0,<4
requests>=2.28
urllib3>=1.26
# Transitive runtime dep via the CLI bridge: bosch_camera imports
# bosch_maintenance, which uses defusedxml.ElementTree (XXE-safe XML).
# Without it, importing the CLI (and the test suite) fails in a fresh env.
defusedxml>=0.7.1
